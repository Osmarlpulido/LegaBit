{
  "name": "legabit",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "openapi:check": "yarn workspace @legabit/api-contracts build && yarn workspace @legabit/backend openapi:check",
    "openapi:compat": "yarn workspace @legabit/backend openapi:compat",
    "openapi:generate": "yarn workspace @legabit/api-contracts build && yarn workspace @legabit/backend openapi:generate",
    "typecheck": "turbo run typecheck",
    "db:generate": "yarn workspace @legabit/db generate",
    "db:migrate": "yarn workspace @legabit/db migrate",
    "db:studio": "yarn workspace @legabit/db studio"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
